import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Context Menu/Installation and setup" />

# Installation and setup

`@perfectmemory/ngx-contextmenu` is built on top of the `@angular/cdk` so you have to install both

## Installation

```shell
npm install @perfectmemory/ngx-contextmenu @angular/cdk
```

```shell
yarn add @perfectmemory/ngx-contextmenu @angular/cdk
```

## Compatibility matrix

| Angular   | @perfectmemory/ngx-contextmenu | Main purpose                                                                  |
| --------- | ------------------------------ | ----------------------------------------------------------------------------- |
| `^20.0.0` | `^20.0.0`                      | Align version with Angular 20                                                 |
| `^19.0.0` | `^19.0.0`                      | Align version with Angular 19                                                 |
| `^18.0.0` | `^18.0.0`                      | Align version with Angular 18                                                 |
| `^17.0.0` | `^17.0.0`                      | Align version with Angular 17                                                 |
| `^16.0.0` | `^16.0.0`                      | Align version with Angular 16                                                 |
| `^15.0.0` | `^15.0.0`                      | Align version with Angular 15                                                 |
| `^14.0.0` | `^14.0.0`                      | Align version with Angular 14                                                 |
| `^13.0.0` | `^8.0.0`                       | Public API cleanup, new features, breaking changes                            |
| `^13.0.0` | `^7.0.0`                       | Support Angular 13                                                            |
| `^12.0.0` | `^6.0.0`                       | Fork from `ngx-contextmenu` (drop in replacement with support for Angular 12) |

## Project setup

In your Angular project, you have to import:

1. Angular CDK overlay and `contextmenu` CSS files
2. `ContextMenuModule` in your Angular application module.

### Import the Angular CDK overlay CSS

You can import it either in the main `styles.scss`, for example `./src/styles.scss`

```css
@import '@angular/cdk/overlay-prebuilt.css';
@import './node_modules/@perfectmemory/ngx-contextmenu/src/assets/stylesheets/base.scss';
```

Or in the `angular.json`

```json
{
  ...
  "architect": {
    "build": {
      "options": {
        "styles": [
          "@angular/cdk/overlay-prebuilt.css",
          "./node_modules/@perfectmemory/ngx-contextmenu/src/assets/stylesheets/base.scss",
          "src/styles.scss",
        ],
      },
    },
  },
  ...
}

```

### Import `ContextMenuModule` in your Angular application module

```ts
import { ContextMenuModule } from '@perfectmemory/ngx-contextmenu';

@NgModule({
  ...
  imports: [
    ContextMenuModule,
  ],
  ...
})
export class AppModule {}
```

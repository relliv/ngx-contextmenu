import { Meta } from '@storybook/addon-docs';

<Meta title="Context Menu/Documentation/ContextMenuService" />

# ContextMenuService

The `ContextMenuService` is an Angular service that can be injected in your component or service.
It provides a `closeAll` method that will close any open context menu. It also provides an `hasOpenMenu` to check if there is at least one.
